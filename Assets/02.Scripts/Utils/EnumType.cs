namespace Utils.EnumType
{
    // 이동 방향
    public enum Direction
    {
        Front,
        Back,
        Right,
        Left
    }

    // 플레이어 상태
    public enum PlayerState
    {
        Idle,
        Move
    }

    // 손님 상태
    public enum CustomerState
    {
        Idle
    }
}